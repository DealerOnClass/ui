/*
 * Feed Mapping Module
 */
.columnmapping {
	display: flex;
	white-space: nowrap;
	overflow-x: auto;
	padding: 2em 4em;
	margin-left: 11em;
}

.columnmapping__item {
	display: inline-flex;
	flex-direction: row;
	flex-shrink: 0;
	width: 14em;
}

.columnmapping__item--header {
	flex-direction: column;
	position: absolute;
	top: 0;
	left: 0;
	padding: 2em;
	background-color: white;
	height: 100%;
	border-right: 1px solid #e4e4e4;
}
